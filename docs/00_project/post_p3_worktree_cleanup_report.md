# Post-P3 working tree cleanup report

Дата: 2026-06-24.

## Scope

Выполнены только этапы `CLEAN.0` и `CLEAN.1` из `prompts/ofz_post_p3_worktree_cleanup_and_next_work_instruction.md`.

Цель этапа: инвентаризировать локальный working tree после Post-P3, удалить только явно generated/local директории и зафиксировать, какие изменения требуют отдельного operator decision.

## Preflight

Команды `git status --short --branch` и `git log --oneline -5` выполнены outside sandbox из корня проекта.

Последние коммиты перед cleanup:

- `dc4176d Document post-P3 release candidate gate`
- `d28641c Plan chart and QA module decomposition`
- `7abf48e Assess post-P3 pipeline optimization opportunities`
- `f92c799 Harden Minfin live dry-run acquisition`
- `2dde514 Assess source registry strict readiness`

## Inventory files

Созданы локальные inventory-файлы в корне проекта:

- `post_p3_worktree_status_before_cleanup.txt`
- `post_p3_modified_files_before_cleanup.txt`
- `post_p3_untracked_files_before_cleanup.txt`
- `post_p3_ignored_files_before_cleanup.txt`

Они не входят в staging scope и рассматриваются как локальные диагностические файлы.

## Working tree before cleanup

По inventory перед cleanup:

| Inventory | Count |
|---|---:|
| `git status --porcelain=v1` | 45 |
| `git diff --name-only` | 25 |
| `git ls-files --others --exclude-standard` | 51 |
| `git ls-files --ignored --others --exclude-standard` | 14363 |

Основные категории:

- `data/raw/minfin/ofz_auction_results/latest/` и `registry/`: modified raw/source registry files, требуют отдельного operator decision.
- `data/raw/minfin/ofz_auction_results/versions/`: untracked version snapshot candidate, raw-zone; не удалялся.
- `outputs/`: generated charts, exports, dashboards, reports, archives and temporary output state.
- `data/processed/`: generated processed datasets.
- `logs/`: generated runtime logs.
- `.ofz_launcher/`: local GUI launcher logs.
- `docs/01_methodology` - `docs/06_quality` и часть `docs/90_archive`: tracked generated/report docs after pipeline and QA runs.
- `prompts/*.md`: untracked instruction files; left untouched for separate classification.

## Dry-run cleanup

Выполнены dry-run команды:

```powershell
git clean -nd -- outputs data/processed logs releases .ofz_launcher
git clean -nd -- outputs/reports/source_acquisition outputs/reports/visual_regression
git clean -nd -- data/raw/minfin/ofz_auction_results/versions
```

Результат:

- `outputs/` showed generated/local files eligible for cleanup.
- `outputs/reports/visual_regression/` showed screenshot artifacts eligible for cleanup.
- `data/raw/minfin/ofz_auction_results/versions/2026/INTERNET_Auction_Results_rus_2026_20260618_3e748e88be0e.xlsx` was shown as a cleanup candidate, but was not deleted because raw versions require operator decision.

## Actual cleanup performed

Удалены только явно разрешенные generated/local директории:

- `outputs/`
- `data/processed/`
- `logs/`
- `.ofz_launcher/`

`releases/` did not exist at cleanup time.

Tracked placeholders/index files under `outputs/` were restored after cleanup so the cleanup does not stage tracked deletions. The resulting state removes local generated contents without turning tracked output scaffolding into a release change.

## Explicitly not touched

The following areas were not changed by cleanup:

- `data/raw/minfin/ofz_auction_results/latest/`
- `data/raw/minfin/ofz_auction_results/registry/`
- `data/raw/minfin/ofz_auction_results/final/`
- `data/raw/minfin/ofz_auction_results/versions/`
- untracked `prompts/*.md`
- root inventory text files, except for being created by the inventory step

## Working tree after cleanup

Remaining working tree categories:

- 3 modified raw/registry files:
  - `data/raw/minfin/ofz_auction_results/latest/INTERNET_Auction_Results_rus_2026_latest.xlsx`
  - `data/raw/minfin/ofz_auction_results/registry/minfin_ofz_auction_sources.csv`
  - `data/raw/minfin/ofz_auction_results/registry/minfin_ofz_auction_sources_latest.json`
- 22 modified tracked docs generated by pipeline/QA reports.
- 1 untracked raw version snapshot:
  - `data/raw/minfin/ofz_auction_results/versions/2026/INTERNET_Auction_Results_rus_2026_20260618_3e748e88be0e.xlsx`
- 5 untracked prompt/instruction markdown files under `prompts/`.
- 4 local inventory text files in the project root.

No generated/local directories from the CLEAN.1 allowlist remain in ignored inventory for `.ofz_launcher`, `logs`, `releases`, `data/processed` or `outputs`.

## Operator decisions still required

1. Decide whether the modified `latest` XLSX and source registry files are an approved source update.
2. Decide whether to keep or remove the untracked raw version snapshot under `data/raw/minfin/ofz_auction_results/versions/2026/`.
3. Classify the untracked prompt/instruction markdown files under `prompts/`: commit as project instructions, move, or remove.
4. Decide whether tracked generated/report docs should be restored or committed as part of a separate reporting update.

## Checks

- `git status --short` - reviewed after cleanup.
- `.\.venv\Scripts\python.exe scripts\qa\check_text_encoding.py` - OK after converting local inventory text files from PowerShell UTF-16 output to UTF-8.
- `git diff --check` - OK.

## Commit scope

Planned staging scope for this step:

- `docs/00_project/post_p3_worktree_cleanup_report.md`

No generated outputs, processed data, logs, release artifacts, `.ofz_launcher` files, source acquisition reports or raw versions are staged.
